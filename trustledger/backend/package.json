{
  "name": "trustledger-backend",
  "version": "0.1.0",
  "private": true,
  "description": "TrustLedger backend API — auth, asset registration, proof bundles",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "express": "^5.2.1",
    "ethers": "^6.13.0",
    "jsonwebtoken": "^9.0.0",
    "@prisma/client": "^5.17.0",
    "multer": "^2.2.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "ts-node-dev": "^2.0.0",
    "prisma": "^5.17.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "@types/jest": "^29.5.0",
    "supertest": "^7.0.0",
    "@types/supertest": "^6.0.0",
    "@types/node": "^20.14.0",
    "@types/express": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.0",
    "@types/multer": "^2.0.0"
  }
}
